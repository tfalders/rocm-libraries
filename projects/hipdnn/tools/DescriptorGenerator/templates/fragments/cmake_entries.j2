## CMake entries for {{ op.name }}

# backend/src/CMakeLists.txt — add to hipdnn_backend_private sources:
    descriptors/{{ op.source_filename }}

# backend/tests/CMakeLists.txt — add to hipdnn_backend_tests sources:
    descriptors/{{ op.test_descriptor_filename }}
    descriptors/{{ op.test_graph_filename }}

# tests/frontend/CMakeLists.txt — add to public_hipdnn_frontend_tests sources:
    {{ op.test_integration_filename }}
    {{ op.test_integration_lifting_filename }}

# backend/tests/CMakeLists.txt — add to hipdnn_backend_tests sources (fromNode tests):
    descriptors/{{ op.test_from_node_filename }}
